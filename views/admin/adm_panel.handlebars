{{!-- <form method="post" action="/admin/create_user">
  <label for="login">Login</label>
  <input id="login" type="text" name="login">
  <label for="password">Senha</label>
  <input id="password" type="password" name="password">
  <button class="btn btn-primary" type="submit">Criar</button>
</form> --}}

<main id="adm_panel_main" class="d-flex justify-content-center flex-wrap">
  <div class="d-flex contact-container flex-column align-items-center">
    <div class="w-100 text-center text-white">
      <h1 class="p-5">Contatos</h1>
    </div>
    {{#each Contact}}
    <div class="contact-box p-5 bg-light border-bottom">
      <div>
        <h3>{{name}}</h3>
        <p>{{petName}}</p>
        <p>{{phone}}</p>
        <p>{{email}}</p>
      </div>
      <div class="contact">
        <div class="btn-group">
          <button type="button" class="btn btn-success">Concluir</button>
          <button type="button" class="btn btn-danger">Excluir</button>
        </div>
      </div>
    </div>
    {{/each}}
  </div>
  <div class="d-flex nav-container flex-column bg-secondary bg-gradient">
    <div class="p-5"></div>
    <ul class="nav nav-pills flex-column align-items-center pt-5 p-3 gap-2">
      <a href="/criar_usuario"><li class="nav-item btn btn-primary">Criar usuário</li></a>
      <a href="/listar_usuario"><li class="nav-item btn btn-primary">Listar usuários</li></a>
    </ul>
  </div>
</main>