{{!-- <form method="post" action="/admin/create_user">
  <label for="login">Login</label>
  <input id="login" type="text" name="login">
  <label for="password">Senha</label>
  <input id="password" type="password" name="password">
  <button class="btn btn-primary" type="submit">Criar</button>
</form> --}}

<main id="adm_panel_main" class="d-flex justify-content-center flex-wrap w-100">
  <div id="error_modal" class="modal" tabindex="-1" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title text-danger">Erro</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <p>Ocorreu um erro ao tentar concluir o contato. Por gentileza contate o administrador.</p>
      </div>
      <div class="modal-footer">
        <button id="error_modal_close" type="button" class="btn btn-secondary" data-bs-dismiss="modal">Fechar</button>
      </div>
    </div>
  </div>
</div>
  <div class="d-flex contact-container flex-column align-items-center">
    <div class="w-100 text-center text-white">
      <h1 class="p-5">Contatos</h1>
    </div>
    {{#if Page}}
    <div class="d-flex flex-row">
      <nav aria-label="Page navigation example">
        <ul class="pagination">
          {{#each Page}}
          <li class="page-item"><a class="page-link text-black" href="/admin/painel/{{number}}">{{number}}</a></li>
          {{/each}}
        </ul>
      </nav>
    </div>
    {{/if}}
    {{#each Contact}}
    <div class="contact-box p-5 bg-light border-bottom" contactId={{id}}>
      <div class="contact-data">
        <h3>{{name}}</h3>
        <p>{{petName}}</p>
        <p>{{phone}}</p>
        <p class="email">{{email}}</p>
      </div>
      <div class="contact">
        <div class="btn-group">
          <button type="button" class="conclude-btn btn btn-success">Concluir</button>
          <button type="button" class="undo-btn btn btn-warning" style="display: none">Desfazer</button>
        </div>
      </div>
    </div>
    {{/each}}
  </div>
  <div class="d-flex nav-container flex-column bg-secondary bg-gradient">
    <div class="p-5"></div>
    <ul class="nav nav-pills flex-column align-items-center pt-5 p-3 gap-2">
      <a href="/criar_usuario"><li class="nav-item btn btn-primary">Criar usuário</li></a>
      <a href="/listar_usuario"><li class="nav-item btn btn-primary">Listar usuários</li></a>
    </ul>
  </div>
</main>